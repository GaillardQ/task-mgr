{% block menu %}
    <!-- http://ironsummitmedia.github.io/startbootstrap-sb-admin/ -->
    <!-- Navigation -->
    <nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
        {{ include ('FrontEndFrontOfficeBundle:Common:menu.html.twig', {}) }}
        
        <!-- Sidebar Menu Items - These collapse to the responsive navigation menu on small screens -->
        <div class="collapse navbar-collapse navbar-ex1-collapse">
            <ul class="nav navbar-nav side-nav">
                <li>
                    <a href="{{ path('projets-index', {}) }}"><i class="fa fa-dashboard"></i>&nbsp&nbspDashboard</a>
                </li>
                <li>
                    <a data-target="#projects-list" data-toggle="collapse" href="javascript:;">
                        <i class="fa fa-fw fa-tags"></i>&nbspListe des projets&nbsp&nbsp<i class="fa fa-fw fa-caret-down"></i>
                    </a>
                    <ul class="collapse" id="projects-list" aria-expanded="false" style="">
                        {% for p in projects %}
                            <li>
                                <a href="{{ path('projets-details', {id: p.infos.id, name: p.infos.name}) }}">{{ p.infos.name }}</a>
                            </li>
                        {% endfor %}
                    </ul>
                </li>
                <li>
                    <a href="{{ path('projets-creation', {}) }}"><i class="fa fa-plus-square"></i>&nbsp&nbspCréation</a>
                </li>
            </ul>
        </div>
        <!-- /.navbar-collapse -->
    </nav>
{% endblock %}